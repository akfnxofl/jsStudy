/* Program?
  : 특정 결과를 얻기 위해 컴퓨터로 실행되는 실행문들의 집합

JavaScript 내에서 실행문은 ';'으로 구분

  ex) var x = 10;
      var result = x + 3;

대소문자 구별

  ex) var js = 10;
      var Js = 20;
      js != Js -> true
      Var js = 30; -> 예약어는 정해진 키워드에 의해서만 동작 (Var X -> var O)

literal : 직접 표현되는 값 그 자체

  ex) 12 -> 숫자 리터럴
      'js' -> 문자열 리터럴
      true -> 불리언 리터럴

식별자 : 변수, 함수의 이름을 작성할 때 사용하는 이름
  -> 영문자(대소문자), 숫자, 언더스코어(_), 달러($) 만 사용 가능

  작성 방법 
    : 1.camelCase -> 이름을 이루는 단어의 첫 글자만 대문자로 작성 (첫 단어는 소문자로 시작)
      2. under_score_case -> 이름을 이루는 단어를 언더스코어(_)로 연결
      */